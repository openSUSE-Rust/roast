feat: add zstd as another alias for zst
prepare structure for roast-scm
minor: improve error message here
ci: add filtering so it only runs when `.rs` and `Cargo.toml` change
dep: update clap features to use
mark dev cycle for v5.2.0
begin cycle for roast-scm development
improvements in the service files
introduce wrap-help feature from clap
fix: added trace feature
run clippy on tests
enable tracing with RUST_LOG in ci
also apply test_log to another test file
use another tracing crate for tests. enable --no-capture
